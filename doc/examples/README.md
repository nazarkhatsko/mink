# Examples

| Example | Description |
|---|---|
| httpbin | Runs against httpbin.org — no local server needed |
| simple-api | Local CRUD API with auth, full user lifecycle |
| order-flow | Multi-step flow demonstrating state mutation and accumulation |

Run `mink doc examples <name>` for details on a specific example.
